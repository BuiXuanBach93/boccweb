CREATE TABLE "SHT_BANNER_PAGE" (
    PAGE_ID                       NUMBER(11)      NOT NULL,
    BANNER_ID                     NUMBER(11),
    PAGE_HEADER                    CLOB,
    PAGE_TITLE                     CLOB,
    PAGE_CONTENT                   CLOB,
    PAGE_FOOTER                    CLOB,
    IMAGE_ID                      NUMBER(11),
    IS_ACTIVE                     NUMBER(1)       DEFAULT 0 NOT NULL,

    CMN_DELETE_FLAG           NUMBER(1)           DEFAULT 0 NOT NULL,
    CMN_ENTRY_DATE            DATE                DEFAULT CURRENT_DATE NOT NULL,
    CMN_LAST_UPDT_DATE        DATE                DEFAULT CURRENT_DATE NOT NULL,
    CMN_ENTRY_USER_NO         NUMBER(11),
    CMN_ENTRY_USER_TYPE       NUMBER(1),
    CMN_LAST_UPDT_USER_NO     NUMBER(11),
    CMN_LAST_UPDT_USER_TYPE   NUMBER(1)
);

ALTER TABLE "SHT_BANNER_PAGE"
    ADD CONSTRAINT SHT_BANNER_PAGE_PK PRIMARY KEY (PAGE_ID);

CREATE SEQUENCE SHT_BANNER_PAGE_SEQ START WITH 1;